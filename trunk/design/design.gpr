with "matreshka/league";
with "asis";

project Design is
   for Object_Dir use "/tmp";
   for Source_Dirs use (".", "api", "a4g", "example");
   for Main use ("test_me");

   package Compiler is
     for Default_Switches ("ada") use
      ("-g", "-gnatwea", "-gnaty", "-gnata");
   end Compiler;

   package Binder is
     for Default_Switches ("ada") use ("-E");
   end Binder;

end Design;

